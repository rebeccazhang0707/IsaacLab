Changed
^^^^^^^

* Allowed cable-load finger rebound at dual-Franka shoelace success, densified the unassisted gripper-opening
  reset curriculum including the contact-release boundary, and promoted after one statistically populated
  frontier window while retaining strict acquisition and release timing. Custom configurations that require the
  former two-window confirmation can set
  ``promotion_window_count`` to ``2``.
